[package]
name = "dummy-client"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1", features = ["macros", "rt-multi-thread", "net"] }
micap-server = { path = "../server" }
anyhow = "1"
glam = "0.28"
